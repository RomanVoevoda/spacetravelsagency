import { Footer, Header, Navbar } from '@/widgets';
import React, { FC } from 'react';

const About: FC = () => {
  return (
    <div>
      <Header />
      <Navbar />
      <Footer />
    </div>
  )
};

export default About;